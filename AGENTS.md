# Agent Working Agreement

本文件对所有参与仓库工作的模型与自动化代理生效。

## 开始任务前

1. 阅读对应 GitHub Issue 的完整内容。
2. 阅读 `docs/GAME_RULES.md` 与 `docs/ARCHITECTURE.md` 中相关章节。
3. 确认 Issue 的“允许修改”和“禁止修改”范围。
4. 若需求、规则或接口不明确，停止编码并在 Issue 中提出问题，不得自行补全关键产品决策。

## 修改范围

- 一个 Issue 对应一个分支和一个 Pull Request。
- 只修改完成 Issue 所必需的文件。
- 不得顺手重构、不相关格式化或升级依赖。
- 不得自行改变公共接口、核心状态结构、依赖方向或规则口径。
- 确需架构变更时，先提交 ADR 建议并等待批准。
- 禁止直接向 `main` 推送。

## 实现约束

- 规则引擎必须是确定性的纯 TypeScript 模块。
- UI 只能提交 Command 并消费 Event，不得复制规则判断。
- 不得读取隐式全局时间或使用 `Math.random()`。
- 状态变化必须原子化：命令失败时不得部分修改状态。
- 新增依赖必须在 Issue 中明确批准并说明用途。
- 不得复制第三方规则书文字、美术、商标或音频。

## 测试要求

- 每个规则功能至少包含正常、非法和边界用例。
- Bug 修复必须先添加能复现问题的回归测试。
- 不得删除、跳过或弱化已有测试来通过 CI。
- 随机场景使用固定 seed。
- 提交前必须运行仓库已有的 `lint`、`typecheck`、`test`、`build` 或统一 `check` 命令。

## 完成报告

Pull Request 描述必须列出：

- 实现内容与明确未实现内容
- 修改文件
- 验收标准逐项对应结果
- 实际执行的命令及结果
- 风险、假设与后续事项

## 审查处理

- 先处理所有阻塞问题，再处理明确要求的非阻塞建议。
- 每次修复说明对应哪条审查意见。
- 不得借修复审查意见扩大功能范围。

